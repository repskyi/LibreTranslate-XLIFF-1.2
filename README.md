# LibreTranslate-XLIFF-1.2
Python program that translates all XLIFF 1.2 files exported from the WPML plugin. Just drop them into to-translate. By default, the program is connected to the local LibreTranslate, but if you update the endpoint, it can connect to an external server
